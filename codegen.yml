overwrite: true
schema: "http://localhost:8000"
documents: "src/graphql/**/*.gql"
generates:
  src/gql/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-vue-urql"
